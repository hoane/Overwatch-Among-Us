void spawnBots(ref define botsToCreate) {
    for (define i = 0; i < botsToCreate; i++) {
        CreateDummyBot(AllHeroes(), Team.All, -1, Vector(-190, 1 + i * 2, -30));
        LastCreatedEntity()
    }
}